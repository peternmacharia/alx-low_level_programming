#include "main.h"

/**
 * _strlen - returns the length of a string
	* @s: this is a string character
 *
	* Return: the length of a string
 */

int _strlen(char *s)
{
	int i;

	i = strlen(*s);

	return (i);
}
