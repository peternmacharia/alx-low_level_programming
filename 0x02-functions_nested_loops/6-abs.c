#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
	*
 * @i: is an integer absolute value
	* 
	* Return: Absolute value
 */
int _abs(int i)
{
if (i >= 0)
{
	return (i);
}
else
{
	return (i * -1);
}
}
