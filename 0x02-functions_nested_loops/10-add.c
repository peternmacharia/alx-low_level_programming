#include "main.h"

/**
 * add - Function that adds two integers
	*
	* @i, @j: this are integers
 * 
	* Return: Returns the result
 */
int add(int i, int j)
{
int result;

result = i + j;
return (result);
}
